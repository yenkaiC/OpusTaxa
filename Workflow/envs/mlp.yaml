name: mlp
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - r-base
    - r-tidyverse
    - r-remotes
    - r-data.table
    - r-nnet
    - r-rpart
    - r-caret
    - r-clock